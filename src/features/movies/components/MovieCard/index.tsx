import { MovieCardMain } from "./MovieCardMain/movieCardMain";
import { MovieImage } from "./MovieCardMain/movieImage";
import { MovieTitle } from "./MovieCardMain/movieTitle";
import { Root } from "./MovieCardMain/root";

export const MovieCard = {
  Root,
  MovieImage,
  MovieTitle,
  MovieCardMain,
}